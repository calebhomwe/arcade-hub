# Chess loop ledger

Append-only. One line per iteration: date | item | change | evidence | commit | not verified.

| date | item | change | evidence | commit | not verified |
|---|---|---|---|---|---|
| 2026-09-13 | B1 save/resume | persisted snapshot (base FEN + flagged move list + clocks), boot replay through executeMove with effects suppressed, Resume wired, cleared on game end | suite saveresume 17/17; per-case 0 fails; one-page 342 checks 0 fails; real Page.reload probe cdp-resume.js: 4 plies + identical FEN before/after, button "Resume", 0 console errors | 77b949f | live build verified: 342 checks, 0 failures, 0 console errors. Not stored: opponent mode/time (a resumed game uses the current settings) |
| 2026-09-13 | B2 PGN import | paste a PGN / move list in the review panel: headers, comments, NAGs, variations, results, SAN, coordinate notation, FEN header base, underpromotion; refused imports leave the game untouched, partial imports keep the readable prefix | suite pgpimport 14/14 | 69fcaf8 | live build verified: 356 checks, 0 failures, 0 console errors || 2026-09-13 | A1 opening book | 40-position book (78 replies) keyed by moves played; legality-filtered lookup falls through to the search; easy limited to the two most common candidates | suite openingbook 11/11 (every key replayed from the start, every reply checked legal); per-case 0 fails; one-page 367 checks 0 fails; live 367/0/0 | 75dc88a | book only covers the main 1.e4/1.d4/1.c4/1.Nf3 families; deeper transpositions fall through to the search |
| 2026-09-13 | A2 endgame conversion | symmetric endgame term (drive the king to the edge, bring yours up, advance passers) + extra search depth when few pieces remain | suite endgameplay 7/7: KQ mates in 47 plies, K+P promotes and mates in 55, KR drives to the edge; evaluation symmetric on 4 endgames; CDP cost 8.2ms opening vs 8.2ms endgame; one-page 374 checks 0 fails; live 374/0/0 | 1bc8177 | KR vs K does not mate inside 80 plies (open follow-up A2b) || 2026-09-13 | B3 FEN load | FEN box in the review panel (prefilled, copyable) with validation: one king each, no pawn on the back rank, non-mover not in check; loads clear the move list, cancel the save and pause the bot | suite fenload 13/13 (exact round trip incl. clocks, castling rights and en passant preserved, 7 invalid positions refused without touching the board); per-case 0 fails; one-page 387 checks 0 fails; live 387/0/0 | 259a03a | the fullmove number is only carried for loaded positions and saved games, not derived from anything else || 2026-09-13 | C1 evaluation bar | horizontal bar under the board driven by the engine evaluation (centre = level, saturates at 10 pawns), off by default, Settings toggle persisted | suite evalbar 9/9 (level 50.0%, queen up 99.3%, queen down 0.8%, pawn 58.5%, follows the game 99.5% -> 50.0%); screenshot read back; one-page 396 checks 0 fails; live 396/0/0 | a18676a | saturates at 10 pawns by design, so huge advantages all look the same || 2026-09-13 | D1 screen-reader announcements | polite live region: side + SAN per move, check, mate with the result; silent during replay/restore/import; cleared on a new game | suite announce 9/9; one-page 405 checks 0 fails; live 405/0/0 | 4bdab0f | announcements are English only (no localisation yet) || 2026-09-13 | A3 bot levels | easy was uniform-random: now a depth-1 search picking among the top four moves within 150cp; medium depth 2, hard depth 3 (unchanged) | measured ACPL vs a depth-3 reference over 9-16 middle-game positions: eas... (line truncated to 2000 chars)
| 2026-09-13 | C2 live move quality | with coaching on every move is graded as played by the same deterministic grader the review uses and marked in the move list (star/tick/?!/??/flame); nothing computed or drawn when coaching is off; marks stored per ply and carried in the game snapshot | suite livetags 8/8 (known answers best/blunder, no marks when off, no grading during import, marks survive save+restore); screenshot read back showing "e4* e5* / Nf3* Nc6* / Bb5tick a6??"; one-page 420 checks 0 fails; live 420/0/0 with both A3 and C2 markers present | 97912c9 | grading costs tens of ms per move while coaching is on; marks are stored but not shown inside the review panel itself (the review recomputes them) || 2026-09-13 | C3 review accuracy + jump | accuracy % per side from the recorded per-move losses (exponential, penalises repeated small slips); a review row now closes the panel and previews that ply instead of drawing an arrow behind a modal | suite reviewfx 9/9 (curve 100/monotonic/clamped, blunder vs slips, real game white 8% vs black 100%, row click previews the right ply and closes the panel); one-page 429 checks 0 fails; live 429/0/0 | cf43d89 | the accuracy curve is my own formula, not chess.com's; it is not comparable to their percentages |
| 2026-09-13 | E2 landscape | two landscape media queries trim restated chrome so the board gets the height; cdp-iphone.js now takes a viewport so any device size can be driven end to end | 844x390: board 270 -> 306px; 932x430: 338px; no horizontal overflow, no control under 40px, touch move + resign + settings all work, 0 console errors; screenshot read back (and caught 'Setti...' truncation, fixed) | 243a4f6 | verification is the device driver, not suite checks; no physical device was used || 2026-09-13 | A3 refinement | one depth rule (searchDepthFor) shared by findBestMove and pickWeakerMove; easy tuned to depth 1 + top six within 250cp + 18% miss chance; levels test asserts the stable blunder-rate signal; endgame test asserts progress rather than a guaranteed mate; sampler restarts short games | measured: easy gives away material on some of 24 moves while medium almost never does (three runs); KQ mates in 31-53 plies in observed runs; per-case 0 failures, one-page 433 checks 0 failures | cce8b4f | the average-centipawn-loss number swings with the sample and is reported, not asserted; KQ/KR endings can still shuffle into a late repetition draw (A2b) || 2026-09-13 | E1 PWA | manifest + icons from the 3D king + service worker (network-first document, cache-first assets, precached shell/mods/pieces/sounds) | offline CDP proof: worker controls the page, network cut, reload boots from cache, rules 24/0, moves 5/0, a move plays, 0 console errors; suite pwa 9/9; one-page 442 checks 0 fails; live 442/0/0 | 98250d9 | cache name is fixed at chess-v1, so installed clients may keep stale assets after a deploy - being audited |
| 2026-09-13 | H1/H2 hygiene | removed 9 unreferenced z*.html debug pages (kept ztest_harness.html, used by the hub queue generator); archived 7 dead chess mods to _loop/archive/legacy-mods so they cannot creep back into loadMods() | nothing referenced them; suite still 442 checks 0 fails after the move | a868056 | other games' scratch files were not touched |
| 2026-09-13 | F2 sound check | Settings button plays every cue in bank order with its name on screen, toggles to Stop, stops on a new game | suite soundcheck 10/10 (enumerates 14 cues in manifest order, first cue named immediately, reset stops it, missing catalogue degrades); one-page 452 checks 0 fails | pending | nobody has still listened to it - that is what this button is for || 2026-09-13 | D2 controls panel | Settings -> Controls: gestures plus nine shortcuts rendered from the same table the keys use; added u undo, r redo, h hint, b eval bar, ? panel | suite help 17/17 (each documented key dispatched and observed), keyboard 6/6, keyboardfull 9/9, a11y 5/5; one-page 468 checks 0 fails | pending | the panel is a static reference, not a guided tour |
| 2026-09-13 | D2 fix | the board key handler initialised the cursor and returned on the FIRST key press of any kind, so the first f/u/h/r did nothing; the cursor is now placed without swallowing the key (and the first arrow press moves rather than only revealing) | help case caught it; two keyboard cases updated to the new semantics; both runners hardened (perft outgrew its 25s virtual budget, one all-in-one load needed more than 18s) | pending | none || 2026-09-13 | PWA staleness | assets moved from cache-first to stale-while-revalidate (serve cached immediately, revalidate in the background); cache bumped to v2 | reproduced the defect first, then proved the fix with an isolated harness that copies the real worker, caches a stub, rewrites it on disk and watches the next fetches: before the fix the client stayed on the old bytes forever, after it serves the cached copy and returns the new content on the next load. Two further bugs found while proving it: the cache key included the query string (every cache-busted request made a new entry) and the background fetch could be answered by Chrome's own HTTP cache | db094ad | deployed worker verified (swr + v2 markers live), live suite 468/0/0 at that point |
| 2026-09-13 | G3 chaos fuzz | 1500 randomised interactions per viewport: pointer drags, right-clicks, touch long-presses, 18 keys, every button id, selects, checkboxes, move-list and review rows, junk into both parsers | 0 uncaught errors and 0 console errors at 488x1055 and again at 393x852 with touch; rules still 24/0 afterwards; reproducible with _loop/tests/run-fuzz.ps1 | d8cdcea | found no defects - the value is the standing guard and the reproducible harness |
| 2026-09-13 | C4 opening name | 39 lines named, keyed by the same prefixes the book plays (a test asserts no orphan names), matched by longest prefix, shown in the review and written as a PGN Opening tag; unknown lines stay unnamed | suite opening 12/12; also fixed a PGN validity bug found on the way (an empty header entry would put a blank line inside the tag section, which terminates it); one-page 480 checks 0 fails; live 480/0/0 with both markers | aabf1d9 | the names are my own plain-English labels, not ECO codes |
| 2026-09-13 | F3 quiet defaults | audit on a fresh profile with an audio spy installed before any page script: nothing is audible before a gesture (0 buffer sources, 0 media plays, 0 oscillators); memes, coaching, the eval bar and the meme panel are all off; a move adds no meme or coaching text; muting is enforced at the source | audit driver _loop/tests/cdp-quiet.js; suite quiet 10/10; one-page 490 checks 0 fails | pending | FINDING NOT FIXED: the sound bank deliberately creates the AudioContext and decodes all 14 cues at load (traced by stack: ac <- _sfxOut <- ensureAudio), so a suspended context and 14 decodes happen before any interaction. Deferring that would risk the first cue being dropped (play() skips cues that are still decoding) and would break the bank case's decode assertion, so the trade-off is recorded rather than changed |
